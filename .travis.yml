#language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t rominam/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rominam/docker-react npm run test -- --coverage